{application, botbop,[
	{description, "websocket bot server"},
	{vsn, "1"},
	{registered, []},
	{applications, [
		kernel,
		stdlib,
		ssl,
		webpage
	]},
	{ modules, [
		botbop_app,
		botbop_sup,
		botbop_server,
		botbop_room
	]},
	{mod, { botbop_app, [ botbop ]}},
	{env, [ ]}
]}.
